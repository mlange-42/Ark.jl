name: Linters

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  linters:
    name: Linters
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: julia-actions/julia-format@v4
      with:
        version: '1'
